package org.firstinspires.ftc.teamcode;

import com.qualcomm.robotcore.eventloop.opmode.TeleOp;

@TeleOp(name="TrollbotTeleOp", group="TrollBot")


public class TrollBotTeleOp
{
    /*
    public void init()
    {
        initialize();
        loop();
    }

    public void loop()
    {
        telemetry.addData("Looping...", "");
        double yL = -gamepad1.left_stick_y;
        double yR = -gamepad1.right_stick_y;
        double motorScale = 1;

        telemetry.addData("loop()", "yL=%f yR=%f", yL, yR);

        if(Math.abs(yL) > 0.2)
        {
            double motorFLPower = leftABSMotorVal(yL) * motorScale;
            motorFL.setPower(motorFLPower);
            telemetry.addData("loop()", "motorFLPower=%f", motorFLPower);
        }
        else
        {
            motorFL.setPower(0);
        }

        if (Math.abs(yR) > 0.2)
        {
            double motorFRPower = rightABSMotorVal(yR) * motorScale;
            motorFR.setPower(motorFRPower);
        }

        else
        {
            motorFR.setPower(0);
        }
    }

     */

}

